// https://umijs.org/config/
import { defineConfig } from '@umijs/max';

export default defineConfig({
  base: '/mss-boot-admin-antd/',
  define: {
    API_URL: 'https://admin-api-alpha.mss-boot-io.top',
  },
});